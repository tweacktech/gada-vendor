import "./chunk-G3PMV62Z.js";

// node_modules/.pnpm/laravel-echo@2.5.0_pusher-js@8.6.0/node_modules/laravel-echo/dist/echo.js
var e = class {
  constructor() {
    this.notificationCreatedEvent = ".Illuminate\\Notifications\\Events\\BroadcastNotificationCreated";
  }
  listenForWhisper(e2, t2) {
    return this.listen(".client-" + e2, t2);
  }
  notification(e2) {
    return this.listen(this.notificationCreatedEvent, e2);
  }
  stopListeningForNotification(e2) {
    return this.stopListening(this.notificationCreatedEvent, e2);
  }
  stopListeningForWhisper(e2, t2) {
    return this.stopListening(".client-" + e2, t2);
  }
};
var t = class {
  constructor(e2) {
    this.namespace = e2;
  }
  format(e2) {
    return [".", "\\"].includes(e2.charAt(0)) ? e2.substring(1) : (this.namespace && (e2 = this.namespace + "." + e2), e2.replace(/\./g, "\\"));
  }
  setNamespace(e2) {
    this.namespace = e2;
  }
};
function n(e2) {
  try {
    return Reflect.construct(String, [], e2), true;
  } catch {
    return false;
  }
}
var r = class extends e {
  constructor(e2, n2, r2) {
    super(), this.name = n2, this.pusher = e2, this.options = r2, this.eventFormatter = new t(this.options.namespace), this.subscribe();
  }
  subscribe() {
    this.subscription = this.pusher.subscribe(this.name);
  }
  unsubscribe() {
    this.pusher.unsubscribe(this.name);
  }
  listen(e2, t2) {
    return this.on(this.eventFormatter.format(e2), t2), this;
  }
  listenToAll(e2) {
    return this.subscription.bind_global((t2, n2) => {
      if (t2.startsWith("pusher:")) return;
      let r2 = String(this.options.namespace ?? "").replace(/\./g, "\\");
      e2(t2.startsWith(r2) ? t2.substring(r2.length + 1) : "." + t2, n2);
    }), this;
  }
  stopListening(e2, t2) {
    return t2 ? this.subscription.unbind(this.eventFormatter.format(e2), t2) : this.subscription.unbind(this.eventFormatter.format(e2)), this;
  }
  stopListeningToAll(e2) {
    return e2 ? this.subscription.unbind_global(e2) : this.subscription.unbind_global(), this;
  }
  subscribed(e2) {
    return this.on("pusher:subscription_succeeded", () => {
      e2();
    }), this;
  }
  error(e2) {
    return this.on("pusher:subscription_error", (t2) => {
      e2(t2);
    }), this;
  }
  on(e2, t2) {
    return this.subscription.bind(e2, t2), this;
  }
};
var i = class extends r {
  whisper(e2, t2) {
    return this.pusher.channels.channels[this.name].trigger(`client-${e2}`, t2), this;
  }
};
var a = class extends r {
  whisper(e2, t2) {
    return this.pusher.channels.channels[this.name].trigger(`client-${e2}`, t2), this;
  }
};
var o = class extends i {
  here(e2) {
    return this.on("pusher:subscription_succeeded", (t2) => {
      e2(Object.keys(t2.members).map((e3) => t2.members[e3]));
    }), this;
  }
  joining(e2) {
    return this.on("pusher:member_added", (t2) => {
      e2(t2.info);
    }), this;
  }
  whisper(e2, t2) {
    return this.pusher.channels.channels[this.name].trigger(`client-${e2}`, t2), this;
  }
  leaving(e2) {
    return this.on("pusher:member_removed", (t2) => {
      e2(t2.info);
    }), this;
  }
};
var s = class extends e {
  constructor(e2, n2, r2) {
    super(), this.events = {}, this.listeners = {}, this.name = n2, this.socket = e2, this.options = r2, this.eventFormatter = new t(this.options.namespace), this.subscribe();
  }
  subscribe() {
    this.socket.emit("subscribe", {
      channel: this.name,
      auth: this.options.auth || {}
    });
  }
  unsubscribe() {
    this.unbind(), this.socket.emit("unsubscribe", {
      channel: this.name,
      auth: this.options.auth || {}
    });
  }
  listen(e2, t2) {
    return this.on(this.eventFormatter.format(e2), t2), this;
  }
  stopListening(e2, t2) {
    return this.unbindEvent(this.eventFormatter.format(e2), t2), this;
  }
  subscribed(e2) {
    return this.on("connect", (t2) => {
      e2(t2);
    }), this;
  }
  error(e2) {
    return this;
  }
  on(e2, t2) {
    return this.listeners[e2] = this.listeners[e2] || [], this.events[e2] || (this.events[e2] = (t3, n2) => {
      this.name === t3 && this.listeners[e2] && this.listeners[e2].forEach((e3) => e3(n2));
    }, this.socket.on(e2, this.events[e2])), this.listeners[e2].push(t2), this;
  }
  unbind() {
    Object.keys(this.events).forEach((e2) => {
      this.unbindEvent(e2);
    });
  }
  unbindEvent(e2, t2) {
    this.listeners[e2] = this.listeners[e2] || [], t2 && (this.listeners[e2] = this.listeners[e2].filter((e3) => e3 !== t2)), (!t2 || this.listeners[e2].length === 0) && (this.events[e2] && (this.socket.removeListener(e2, this.events[e2]), delete this.events[e2]), delete this.listeners[e2]);
  }
};
var c = class extends s {
  whisper(e2, t2) {
    return this.socket.emit("client event", {
      channel: this.name,
      event: `client-${e2}`,
      data: t2
    }), this;
  }
};
var l = class extends c {
  here(e2) {
    return this.on("presence:subscribed", (t2) => {
      e2(t2.map((e3) => e3.user_info));
    }), this;
  }
  joining(e2) {
    return this.on("presence:joining", (t2) => e2(t2.user_info)), this;
  }
  whisper(e2, t2) {
    return this.socket.emit("client event", {
      channel: this.name,
      event: `client-${e2}`,
      data: t2
    }), this;
  }
  leaving(e2) {
    return this.on("presence:leaving", (t2) => e2(t2.user_info)), this;
  }
};
var u = class extends e {
  subscribe() {
  }
  unsubscribe() {
  }
  listen(e2, t2) {
    return this;
  }
  listenToAll(e2) {
    return this;
  }
  stopListening(e2, t2) {
    return this;
  }
  subscribed(e2) {
    return this;
  }
  error(e2) {
    return this;
  }
  on(e2, t2) {
    return this;
  }
};
var d = class extends u {
  whisper(e2, t2) {
    return this;
  }
};
var f = class extends u {
  whisper(e2, t2) {
    return this;
  }
};
var p = class extends d {
  here(e2) {
    return this;
  }
  joining(e2) {
    return this;
  }
  whisper(e2, t2) {
    return this;
  }
  leaving(e2) {
    return this;
  }
};
var _a;
var m = (_a = class {
  constructor(e2) {
    this.setOptions(e2), this.connect();
  }
  setOptions(t2) {
    this.options = {
      ..._a._defaultOptions,
      ...t2,
      broadcaster: t2.broadcaster
    };
    let n2 = this.csrfToken();
    n2 && (this.options.auth.headers["X-CSRF-TOKEN"] = n2, this.options.userAuthentication.headers["X-CSRF-TOKEN"] = n2), n2 = this.options.bearerToken, n2 && (this.options.auth.headers.Authorization = "Bearer " + n2, this.options.userAuthentication.headers.Authorization = "Bearer " + n2);
  }
  csrfToken() {
    return typeof window < "u" && window.Laravel?.csrfToken ? window.Laravel.csrfToken : this.options.csrfToken ? this.options.csrfToken : typeof document < "u" && typeof document.querySelector == "function" ? document.querySelector('meta[name="csrf-token"]')?.getAttribute("content") ?? null : null;
  }
}, _a._defaultOptions = {
  auth: { headers: {} },
  authEndpoint: "/broadcasting/auth",
  userAuthentication: {
    endpoint: "/broadcasting/user-auth",
    headers: {}
  },
  csrfToken: null,
  bearerToken: null,
  host: null,
  key: null,
  namespace: "App.Events"
}, _a);
var h = class extends m {
  constructor(...e2) {
    super(...e2), this.channels = {};
  }
  connect() {
    if (this.options.client !== void 0) this.pusher = this.options.client;
    else if (this.options.Pusher) this.pusher = new this.options.Pusher(this.options.key, this.options);
    else if (typeof window < "u" && window.Pusher !== void 0) this.pusher = new window.Pusher(this.options.key, this.options);
    else throw Error("Pusher client not found. Should be globally available or passed via options.client");
  }
  signin() {
    this.pusher.signin();
  }
  listen(e2, t2, n2) {
    return this.channel(e2).listen(t2, n2);
  }
  channel(e2) {
    return this.channels[e2] || (this.channels[e2] = new r(this.pusher, e2, this.options)), this.channels[e2];
  }
  privateChannel(e2) {
    return this.channels["private-" + e2] || (this.channels["private-" + e2] = new i(this.pusher, "private-" + e2, this.options)), this.channels["private-" + e2];
  }
  encryptedPrivateChannel(e2) {
    return this.channels["private-encrypted-" + e2] || (this.channels["private-encrypted-" + e2] = new a(this.pusher, "private-encrypted-" + e2, this.options)), this.channels["private-encrypted-" + e2];
  }
  presenceChannel(e2) {
    return this.channels["presence-" + e2] || (this.channels["presence-" + e2] = new o(this.pusher, "presence-" + e2, this.options)), this.channels["presence-" + e2];
  }
  leave(e2) {
    [
      e2,
      "private-" + e2,
      "private-encrypted-" + e2,
      "presence-" + e2
    ].forEach((e3) => {
      this.leaveChannel(e3);
    });
  }
  leaveChannel(e2) {
    this.channels[e2] && (this.channels[e2].unsubscribe(), delete this.channels[e2]);
  }
  socketId() {
    return this.pusher.connection.socket_id;
  }
  connectionStatus() {
    let e2 = this.pusher.connection.state;
    switch (e2) {
      case "connected":
      case "connecting":
        return e2;
      case "failed":
      case "unavailable":
        return "failed";
      default:
        return "disconnected";
    }
  }
  onConnectionChange(e2) {
    let t2 = () => {
      e2(this.connectionStatus());
    }, n2 = [
      "state_change",
      "connected",
      "disconnected"
    ];
    return n2.forEach((e3) => {
      this.pusher.connection.bind(e3, t2);
    }), () => {
      n2.forEach((e3) => {
        this.pusher.connection.unbind(e3, t2);
      });
    };
  }
  disconnect() {
    this.pusher.disconnect();
  }
};
var g = class extends m {
  constructor(...e2) {
    super(...e2), this.channels = {};
  }
  connect() {
    let e2 = this.getSocketIO();
    this.socket = e2(this.options.host ?? void 0, this.options), this.socket.io.on("reconnect", () => {
      Object.values(this.channels).forEach((e3) => {
        e3.subscribe();
      });
    });
  }
  getSocketIO() {
    if (this.options.client !== void 0) return this.options.client;
    if (typeof window < "u" && window.io !== void 0) return window.io;
    throw Error("Socket.io client not found. Should be globally available or passed via options.client");
  }
  listen(e2, t2, n2) {
    return this.channel(e2).listen(t2, n2);
  }
  channel(e2) {
    return this.channels[e2] || (this.channels[e2] = new s(this.socket, e2, this.options)), this.channels[e2];
  }
  privateChannel(e2) {
    return this.channels["private-" + e2] || (this.channels["private-" + e2] = new c(this.socket, "private-" + e2, this.options)), this.channels["private-" + e2];
  }
  presenceChannel(e2) {
    return this.channels["presence-" + e2] || (this.channels["presence-" + e2] = new l(this.socket, "presence-" + e2, this.options)), this.channels["presence-" + e2];
  }
  leave(e2) {
    [
      e2,
      "private-" + e2,
      "presence-" + e2
    ].forEach((e3) => {
      this.leaveChannel(e3);
    });
  }
  leaveChannel(e2) {
    this.channels[e2] && (this.channels[e2].unsubscribe(), delete this.channels[e2]);
  }
  socketId() {
    return this.socket.id;
  }
  connectionStatus() {
    return this.socket.connected ? "connected" : this.socket.io._reconnecting ? "reconnecting" : this.socket.id === void 0 ? "connecting" : "disconnected";
  }
  onConnectionChange(e2) {
    let t2 = () => {
      e2(this.connectionStatus());
    }, n2 = [
      "connect",
      "disconnect",
      "connect_error",
      "reconnect_attempt",
      "reconnect",
      "reconnect_error",
      "reconnect_failed"
    ];
    return n2.forEach((e3) => {
      this.socket.on(e3, t2);
    }), () => {
      n2.forEach((e3) => {
        this.socket.off(e3, t2);
      });
    };
  }
  disconnect() {
    this.socket.disconnect();
  }
};
var _ = class extends m {
  constructor(...e2) {
    super(...e2), this.channels = {};
  }
  connect() {
  }
  listen(e2, t2, n2) {
    return new u();
  }
  channel(e2) {
    return new u();
  }
  privateChannel(e2) {
    return new d();
  }
  encryptedPrivateChannel(e2) {
    return new f();
  }
  presenceChannel(e2) {
    return new p();
  }
  leave(e2) {
  }
  leaveChannel(e2) {
  }
  socketId() {
    return "fake-socket-id";
  }
  connectionStatus() {
    return "connected";
  }
  onConnectionChange(e2) {
    return () => {
    };
  }
  disconnect() {
  }
};
var v = class {
  constructor(e2) {
    this.options = e2, this.connect(), this.options.withoutInterceptors || this.registerInterceptors();
  }
  channel(e2) {
    return this.connector.channel(e2);
  }
  connect() {
    if (this.options.broadcaster === "reverb") this.connector = new h({
      ...this.options,
      cluster: ""
    });
    else if (this.options.broadcaster === "pusher") this.connector = new h(this.options);
    else if (this.options.broadcaster === "ably") this.connector = new h({
      ...this.options,
      cluster: "",
      broadcaster: "pusher"
    });
    else if (this.options.broadcaster === "socket.io") this.connector = new g(this.options);
    else if (this.options.broadcaster === "null") this.connector = new _(this.options);
    else if (typeof this.options.broadcaster == "function" && n(this.options.broadcaster)) this.connector = new this.options.broadcaster(this.options);
    else throw Error(`Broadcaster ${typeof this.options.broadcaster} ${String(this.options.broadcaster)} is not supported.`);
  }
  disconnect() {
    this.connector.disconnect();
  }
  join(e2) {
    return this.connector.presenceChannel(e2);
  }
  leave(e2) {
    this.connector.leave(e2);
  }
  leaveChannel(e2) {
    this.connector.leaveChannel(e2);
  }
  leaveAllChannels() {
    for (let e2 in this.connector.channels) this.leaveChannel(e2);
  }
  listen(e2, t2, n2) {
    return this.connector.listen(e2, t2, n2);
  }
  private(e2) {
    return this.connector.privateChannel(e2);
  }
  encryptedPrivate(e2) {
    if (this.connectorSupportsEncryptedPrivateChannels(this.connector)) return this.connector.encryptedPrivateChannel(e2);
    throw Error(`Broadcaster ${typeof this.options.broadcaster} ${String(this.options.broadcaster)} does not support encrypted private channels.`);
  }
  connectorSupportsEncryptedPrivateChannels(e2) {
    return e2 instanceof h || e2 instanceof _;
  }
  socketId() {
    return this.connector.socketId();
  }
  connectionStatus() {
    return this.connector.connectionStatus();
  }
  registerInterceptors() {
    typeof Vue < "u" && Vue?.http && this.registerVueRequestInterceptor(), typeof axios == "function" && this.registerAxiosRequestInterceptor(), typeof jQuery == "function" && this.registerjQueryAjaxSetup(), typeof Turbo == "object" && this.registerTurboRequestInterceptor();
  }
  registerVueRequestInterceptor() {
    Vue.http.interceptors.push((e2, t2) => {
      this.socketId() && e2.headers.set("X-Socket-ID", this.socketId()), t2();
    });
  }
  registerAxiosRequestInterceptor() {
    axios.interceptors.request.use((e2) => (this.socketId() && (e2.headers["X-Socket-Id"] = this.socketId()), e2));
  }
  registerjQueryAjaxSetup() {
    jQuery.ajax !== void 0 && jQuery.ajaxPrefilter((e2, t2, n2) => {
      this.socketId() && n2.setRequestHeader("X-Socket-Id", this.socketId());
    });
  }
  registerTurboRequestInterceptor() {
    document.addEventListener("turbo:before-fetch-request", (e2) => {
      e2.detail.fetchOptions.headers["X-Socket-Id"] = this.socketId();
    });
  }
};
export {
  e as Channel,
  m as Connector,
  t as EventFormatter,
  v as default
};
//# sourceMappingURL=laravel-echo.js.map
