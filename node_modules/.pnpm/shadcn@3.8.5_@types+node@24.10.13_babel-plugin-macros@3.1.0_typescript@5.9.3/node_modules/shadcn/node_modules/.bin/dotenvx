#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../../@dotenvx/dotenvx/src/cli/dotenvx.js" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../@dotenvx/dotenvx/src/cli/dotenvx.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../../@dotenvx/dotenvx/src/cli/dotenvx.js" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../../@dotenvx/dotenvx/src/cli/dotenvx.js" "$@"
else
  exec node  "$basedir/../../../@dotenvx/dotenvx/src/cli/dotenvx.js" "$@"
fi
# cmd-shim-target=/Users/user/Project/GadaTech/my-vendor/node_modules/.pnpm/shadcn@3.8.5_@types+node@24.10.13_babel-plugin-macros@3.1.0_typescript@5.9.3/node_modules/@dotenvx/dotenvx/src/cli/dotenvx.js
